# MailProxy 自建邮箱后台 · 部署说明

> 最后更新：2026-09-21。本文与当前代码（`src/后端.js` + `frontend/index.html`）保持一致。

## 一、项目简介

纯免费、**无 VPS、不绑卡**，全程跑在 Cloudflare 免费套餐。

把你的域名（示例 `your-domain.example.com`）做成一体化后台：**自建邮箱（收/发）+ 云盘 + 通讯录 + 记事本**。
核心诉求：无限免费、难被拦截；对外发信用 `@your-domain.example.com` 身份，对方看不到你的真实邮箱。

> 命名约定：磁盘文件名用中文，**代码内 API 路由全部英文**（如 `/api/login`、`/api/send`、`/api/messages`、`/api/drive`、`/api/share`）。

## 二、功能清单

- **收件箱**：列表 / 读信（实时解析纯文本正文、RFC2047 中文主题、真实发件人）/ 多选批量删 / 软删除（「已删除」可恢复）/ 永久删
- **写邮件**：以任意 `@your-domain.example.com` 别名发信、附件（XHR 真实进度、发送中锁定）、存草稿
- **已发送 / 草稿箱**：记录与续编
- **邮箱别名**：新建 / 删除 / 头像（裁剪 128px）/ 星标
- **通讯录 / 重要联系人**：增删改查
- **云盘（drive）**：上传（二进制直传，单文件 ≤25MB，真实进度）/ 列表 / 改名 / 删 / 预览 / **分享**（可选提取码 + 有效期 + 统计下载人数）
- **记事本**：增删改查
- **其他应用**：侧栏底部外链入口（默认空，可在 `frontend/index.html` 的 `otherapps-pop` 中按示例自行添加跳转）
- **通用**：深浅主题（localStorage 记忆）、toast 堆叠、确认弹窗、手机端自适应

## 三、技术栈

| 层 | 技术 |
|---|---|
| 运行 | Cloudflare Workers（`src/后端.js`，`compatibility_date = "2024-11-01"`） |
| 邮件库 | D1 `DB`（库名 `mailproxy`） |
| 云盘库 | D1 `DRV`（库名 `drive`） |
| 前端 | `frontend/index.html`（单文件 SPA，`GET /` 由 assets 返回，与 API 同源） |
| 收信 | Email Routing catch-all → Worker → ① 转发真实邮箱 ② 存 D1（双写，不丢） |
| 发信 | Resend（优先）/ MailChannels Email API（备用），支持附件 |
| 域名 | 自定义域 `your-domain.example.com`，`workers_dev = false` |
| 定时 | Cron：每日北京时间 04:00 清理「已删除 >30 天邮件」+「过期分享」 |

## 四、前置准备

1. 一个 Cloudflare 账号（免费套餐，无需绑卡）。
2. 本机装 Node.js，然后 `npm install -g wrangler`（或用 `npx wrangler`）。
3. 准备以下环境变量（均为 secret，**不进代码**，用 `wrangler secret put` 设置）：
   - `ADMIN_TOKEN`：后台登录密码（即登录后拿到的内部 token）。例：`openssl rand -hex 16`
   - `ADMIN_ACCOUNT`：**可选**，登录账号名，默认 `admin`（大小写不敏感；不出现在前端页面上）
   - `DOMAIN`：**可选**，你的域名（如 `your-domain.example.com`），用于发信身份与界面展示；不填则用代码内占位值
   - `QQ_EMAIL`：收信转发目标，如 `you@example.com`
   - `RESEND_API_KEY`：发信通道 1（Resend 免费 100 封/天），**可选但推荐**
   - `MAILCHANNELS_API_KEY`：发信通道 2（MailChannels Email API），**可选**

## 五、建数据库（两个 D1 库）

表结构已补全在 `数据库结构.sql`（含邮件主库 6 张表 + 云盘库 3 张表）。
分别在两个库各执行一次（本地 + 远程）：

```bash
# 邮件主库
wrangler d1 execute mailproxy --remote --file=./数据库结构.sql
wrangler d1 execute mailproxy --local  --file=./数据库结构.sql
# 云盘库
wrangler d1 execute drive     --remote --file=./数据库结构.sql
wrangler d1 execute drive     --local  --file=./数据库结构.sql
```

> 注意：先 `wrangler d1 create mailproxy` 和 `wrangler d1 create drive` 创建两个库，把返回的 id 填进 `wrangler.toml` 的 `database_id`。
> `wrangler.toml` 里的 `database_name` 是 `mailproxy` 与 `drive`，务必与 `wrangler d1 create` 时起的名字一致。

## 六、wrangler.toml

已就位（**已去除具体账号与数据库 ID，开源安全**），关键项：
- `name = "mailproxy-worker"`，`main = "src/后端.js"`
- `account_id` 已注释掉，部署前填你自己的 Cloudflare 账号 ID，或 `wrangler login` 自动识别
- `workers_dev = false`（关掉 `<sub>.workers.dev` 子域，避免泄露；如需直接访问可改 `true`）
- `assets = { directory = "./frontend", binding = "ASSETS" }`（前端静态资源）
- `[[d1_databases]]` 绑定 `DB`(mailproxy) 与 `DRV`(drive)，`database_id` 需自行填入
- `[triggers] crons = ["0 20 * * *"]`（UTC，对应北京 04:00）

> 自定义域请自行在 Cloudflare 面板绑定到本 Worker（或 `wrangler route`）。

## 七、设密钥（不进代码）

```bash
wrangler secret put ADMIN_TOKEN
wrangler secret put QQ_EMAIL
wrangler secret put RESEND_API_KEY
wrangler secret put MAILCHANNELS_API_KEY
# 可选：
wrangler secret put ADMIN_ACCOUNT
wrangler secret put DOMAIN
# 分别粘贴对应值
```

## 八、部署 Worker

```bash
cd mailproxy
npx wrangler deploy
# 部署成功自动生效到你的自定义域
```

> 本地改动 `frontend/index.html` 后，直接 `wrangler deploy` 即可（assets 目录整体上传，无需重建）。
> 部署需要目标 Cloudflare 账号的 API Token；可用环境变量传入：
> `CLOUDFLARE_API_TOKEN=<你的 Cloudflare API Token> npx wrangler deploy`

## 九、DNS 记录（Cloudflare 面板）

自定义域 `your-domain.example.com` 激活后，还需：

| 类型 | 名称 | 内容 | 说明 |
|---|---|---|---|
| TXT | `@`（根） | `v=spf1 include:relay.mailchannels.net ~all` | **合并进现有 SPF**，不能新建第二条 SPF（把原 SPF 的 include 后追加 ` include:relay.mailchannels.net`） |
| TXT | `_mailchannels` | `v=mc1 cfid=<你的sub>.workers.dev` | Domain Lockdown，MailChannels 校验域所有权。**必填**，否则发信被拒 |
| TXT | `_dmarc` | `v=DMARC1; p=none` | 提升信任（可选但建议） |

> DKIM：MailChannels 不强制你配 DKIM（靠 SPF + Domain Lockdown 发信，自带签名）。不加也能发；想更稳可忽略。

## 十、catch-all 改路由到 Worker（关键）

Cloudflare → 电子邮件 → 电子邮件路由 → 路由规则 → 编辑「全收（Catch-all）」：
- 操作从「发送到电子邮件」改为「**发送到 Worker**」；
- 选 `mailproxy-worker`；
- 保存。

改后逻辑：信 → Worker → ① `message.forward(真实邮箱)` 继续进真实邮箱 ② 存 D1 供后台查看。原信一封不丢。

## 十一、使用管理前台

直接访问你的自定义域（同源 SPA，无需单独配置前台地址）：
- 登录页只填**账号**（默认 `admin`，若设了 `ADMIN_ACCOUNT` 则用该值），页面不显示账号名；登录后拿到 token 供后续 API 鉴权。
- 左侧栏：收件箱 / 发件箱（折叠：已发送、草稿）/ 其他（星标、已删除、垃圾箱、重要联系人）/ 应用（云盘、通讯录、记事本）/ 其他应用（外链，默认空，可自行添加）。

## 十二、验证

1. 用别的邮箱发 `test@your-domain.example.com` → 真实邮箱收到 + 后台「收件箱」也能看到 = 双写成功。
2. 后台写一封发出 → 对方收到、信头 `From` 是你域名、`Return-Path` 是 mailchannels 系、无真实邮箱 = 隐私成功。
3. 云盘上传一个文件 → 生成分享链接 `/s/<token>` → 用无痕窗口打开 →（若有提取码）输入后下载 = 分享成功。

## 十三、红线提醒

MailChannels **严禁批量 / 营销 / 订阅推送**，个人事务性偶发完全 OK。违规会封发信通道、毁域名信誉。

## 十四、数据模型速查

**邮件主库 `DB` (mailproxy)**

| 表 | 关键字段 |
|---|---|
| messages | id, from_addr, to_addr, subject, body_text, raw, received_at, deleted, deleted_at |
| addresses | local(PK), note, created_at, avatar, starred |
| sent | id, from_addr, to_addr, subject, body_text, sent_at |
| drafts | id, from_addr, to_addr, subject, body_text, updated_at |
| contacts | addr(PK), name, note, created_at |
| notes | id, title, content, updated_at |

**云盘库 `DRV` (drive)**

| 表 | 关键字段 |
|---|---|
| files | id, name, size, type, created_at |
| file_chunks | id, file_id, idx, data(BLOB / 回退 base64 TEXT) |
| shares | token(PK), file_id, code, created_at, expires_at, downloads |

## 十五、定时任务

Cron `0 20 * * *`（UTC，北京 04:00）每日执行：
- 清理 `messages` 中 `deleted=1 且 deleted_at` 早于 30 天的邮件；
- 清理 `shares` 中 `expires_at` 已过期的分享链接。

## 十六、主要 API（供参考）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/login` | 账号（默认 admin）登录，返回内部 token |
| GET | `/api/messages?deleted=0/1` | 列信（不含正文） |
| GET | `/api/messages/:id` | 读信（实时解析正文） |
| DELETE | `/api/messages/:id?purge=1` | 删信（软删 / 永久删） |
| POST | `/api/messages/bulk-delete` | 批量软删 / 永久删 |
| POST | `/api/messages/:id` | 恢复（已删除→收件箱） |
| POST | `/api/send` | 发信（双通道，附件 ≤40MB） |
| GET/POST/DELETE | `/api/sent[/:id]` | 已发送 |
| GET/POST/DELETE | `/api/drafts[/:id]` | 草稿箱 |
| GET/POST/DELETE | `/api/addresses[/:local]` | 邮箱别名（含头像/星标） |
| GET/POST/DELETE | `/api/contacts[/:addr]` | 通讯录 |
| GET/POST/DELETE | `/api/notes[/:id]` | 记事本 |
| GET/POST/DELETE | `/api/drive[/:id]` | 云盘（列 / 上传 / 改名 / 删） |
| GET | `/api/drive/:id?inline=1&tk=` | 下载 / 预览（tk 免登录） |
| POST | `/api/drive/:id/share` | 创建分享（可选 code + expires） |
| GET/DELETE | `/api/drive/:id/shares` | 分享记录（含 downloads）/ 取消 |
| GET | `/api/share/:token?code=` | 公开分享信息（错码不返回文件名） |
| GET | `/api/share/:token/download?code=` | 公开下载（downloads+1） |

> 分享落地页：`/s/<token>`（或 `/s/<token>=<提取码>` 免输码）、带主题切换、原生浏览器下载、过期返回 410。
